Things to Know prior to running
  To run the webmap it requires your own  API Keys
To Run the Program
  1st you have to generate campgrounds by running "index.js" from the "seeds" folder(its inside yelpcamp)
  Then Run the outer "index.js" file in yelpcamp for it run like its supposed to.

